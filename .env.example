# Copy to .env and fill in. Vista Social Integration API key.
# Find it in Vista Social → Settings → Account Settings → Integrations.
# Read server-side by the Vite dev proxy (vite.config.js); never sent to the browser.
VISTA_SOCIAL_API_KEY=
